# dbt tooling for the Bronze (Fabric Lakehouse) -> Silver (Fabric Warehouse)
# normalization layer.
#
# 2026-09-01: dbt-core/dbt-fabric are now ALSO pinned directly in the main
# requirements.txt -- the app's own image needs dbt installed for
# src/lakehouse/fabric_dbt_runner.py's run_dbt_silver_build() to actually
# run (it silently no-op'd in every prior deploy, since dbt was never
# installed in that image). A clean-venv dry-run confirmed no real
# version conflict with the app's other pins, despite this file's
# original isolation rationale. This file is kept only as a convenience
# for installing dbt alone (e.g. a standalone dbt-only venv, without
# pulling in the rest of the app's dependencies):
#   venv/Scripts/python.exe -m pip install -r dbt/requirements-dbt.txt
dbt-core==1.12.3
dbt-fabric==1.11.1
