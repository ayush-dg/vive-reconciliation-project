// Root route placeholder — Task 1.1 (repository scaffolding) only.
// Task 1.3 (Sign In) wires the real unauthenticated-root -> /login redirect
// once /login exists; redirecting to it before then would 404.
export default function RootPage() {
  return <p>VIVE Statement Reconciliation — scaffold placeholder.</p>;
}
