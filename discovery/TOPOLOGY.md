## TOPOLOGY.md — VIVE Reconciliation
Produced by: BCE Stage 2 Session A (CC) — Path A (Custodian-Led), fresh extraction
Date: 2026-08-05

Assembled from A01 (Layer Boundary Map), A02 (Module Call Map — full detail in
`discovery/components/A02_module_call_map.md`), and A03 (External System Boundary Map).
This is a **fresh baseline**, not an update to the archived (`discovery/_archive_2026-07/`)
TOPOLOGY.md — every field below was re-derived from a direct read of current source, not
carried forward from the archive. Where this pass's findings differ from what
`discovery/INTAKE_SUMMARY.md` stated, the divergence is marked explicitly below per the Divergence rule — neither source
is silently preferred.

---

## A01 — Layer Boundary Map

| # | Produced by | Artifact | Consumed by | Handoff mechanism | Failure mode |
|---|---|---|---|---|---|
| 1 | M-012 upload router (HTTP handler) or M-015 intake_trigger webhook (Event Grid) | PDF file (saved to `sample_data/`) + `jobs` row (PENDING) | M-005 background worker pool (`VIVE_WORKER_POOL_SIZE` threads, default 3) | File system + database row, polled every 30s per pool thread | No stale-job requeue exists if a job is stuck PROCESSING. The claim guard (`claim_next_pending_job()`, M-003) is scoped per `pdf_filename`, not system-wide — a stuck job only blocks new claims for that same filename; every other filename can still be claimed. Confirmed directly against `web/queries.py:761-812`. |
| 2 | M-005 background worker | Subprocess invocation | M-021 full pipeline orchestrator (`scripts/run_full_pipeline.py`) | `subprocess.run(..., timeout=1800)`, captured stdout/stderr | Non-zero exit or missing `"Statement ID:"` marker in output → job FAILED with truncated (4000 char) error message. 30-minute hard cap. Within the subprocess, the Claude Sonnet call (M-025) is additionally gated by M-041's cross-process concurrency limiter. |
| 3 | M-017 document intake (`notebooks/01_document_intake.py`) | `bronze_vendor_statement_raw` + `silver_reconciliation_standard` (VENDOR_STATEMENT) rows | M-035 mock ERP generator | Direct database read (same DB, same statement_id) | `generate_mock_erp()` raises `ValueError` if zero Silver VENDOR_STATEMENT rows exist for the statement_id — confirmed at `src/mock_erp/generator.py:77-79`. **Also confirmed:** `extract_pdf_text()` now raises `CorruptedPDFError` (not a raw pdfminer/pdfplumber exception) on an unopenable PDF, caught at both `run_full_pipeline.py`'s `main()` and `01_document_intake.py`'s own `__main__` block, producing a clean exit(1) message instead of a raw traceback — this is new since the archived TOPOLOGY.md (added `7e2d811`). |
| 4 | M-035 mock ERP generator | `bronze_internal_erp_raw` + `silver_reconciliation_standard` (INTERNAL_ERP) rows | M-034 matching engine | Direct database read | `run_matching()` raises `ValueError` if zero Silver INTERNAL_ERP rows exist — confirmed at `src/matching/engine.py:256-260`. |
| 5 | M-034 matching engine | `gold_matched_invoices` / `gold_exceptions` / `gold_reconciliation_summary` | M-003 web query layer, M-020 report entry point | Direct database read | **Confirmed fixed, not just resolved-in-2026-07:** `gold_reconciliation_summary.exception_count`/`overall_status` used to go stale after an exception was resolved outside a full re-match (archived `RISK_REGISTER.md` implied this was still a live gap). Directly verified in `web/queries.py:488-540` (`_recompute_summary_counts()`, called from `resolve_exception()` and `action_review_item()`) and `src/matching/engine.py:118-128` (`score_overall_status()`, the shared tiering both call sites use) — this was fixed in commit `3cc3c37`, after the archived TOPOLOGY.md's last update. The dashboard KPI query (`get_kpis()`) still also independently wraps every `SUM(...)` in `COALESCE(..., 0)`, degrading gracefully on an empty table — unrelated mechanism, same conclusion (no guard gap). |
| 6 | M-024 document understanding engine (primary AI provider path) | Universal Financial Document Schema dict | M-017 document intake (`validate_invoice`, `write_to_bronze`) | In-process Python return value | Primary provider exception is caught; falls back to M-031 pdfplumber deterministically. If that also raises, `_failed_schema()` returns a zero-invoice, `UNKNOWN`-type schema rather than propagating — `run_full_pipeline.py` explicitly detects `bronze_count == 0` and prints "PIPELINE STOPPED — no invoices extracted" rather than crashing. Confirmed unchanged from the archived record. |
| 7 | M-043 Blob Storage client | Archived PDF blob URL | M-017 document intake (`update_intake_log_blob_path`) | In-process function return value | Never raises — any failure (missing config, missing file, network/auth) returns `None`; intake logs a warning and continues. Confirmed unchanged (`src/storage/blob_client.py:69-123`). |
| 8 | **[NEW]** M-017 document intake / M-003 web query layer | `extraction_cache`, `document_intake_log`, `validation_document_review_queue` rows | M-037 lakehouse connection's Fabric path | `execute_sql_fabric()`/`execute_query_fabric()` — no dialect translation, no drop-retry (unlike `execute_sql()`/`execute_query()`) | These three tables' `id` column has no `IDENTITY` on Fabric — every write site computes `MAX(id) + 1` explicitly in Python, which is **not concurrency-safe**: two callers writing to the same table at the same instant could compute the same next id. Confirmed directly in `notebooks/01_document_intake.py`'s `update_cache()`/`write_to_review_queue()`/`write_intake_log()` docstrings and code (lines 290-330, 425-497, 579-635). This is a real, currently-unmitigated risk introduced by the Fabric cut-over, not present in the archived record at all. |

**[STAGE-2-DIVERGENCE — 2026-08-05]:** `discovery/INTAKE_SUMMARY.md`'s Known Pain Points section (drafted earlier this session from `src/lakehouse/connection.py`'s docstring alone) states the Fabric migration is *"scoped so far to just `extraction_cache`."* Direct source verification in this session shows that is **incomplete** — `web/queries.py` (`get_vendor_name_for_statement()`, `get_statement_report()`, `get_pending_review_count()`, `get_review_queue_vendors()`, `get_review_queue_for_vendor()`, `get_review_queue_item()`, `action_review_item()`) and `notebooks/01_document_intake.py` (`check_cache()`, `write_to_review_queue()`, `write_intake_log()`, `update_intake_log_blob_path()`, `update_cache()`) all call `execute_query_fabric()`/`execute_sql_fabric()` against **three** tables: `extraction_cache`, `document_intake_log`, and `validation_document_review_queue`. **Resolution required before Stage 3:** `INTAKE_SUMMARY.md`'s Known Pain Points bullet and Open Question on this topic should be corrected to reflect the actual three-table scope, and the concurrency-safety gap on the Fabric tables' `id` assignment (A01 row 8 above) should be considered for a RISK_REGISTER entry once Session E runs.

---

## A02 — Module Call Map

Full Module Roster (50 modules, M-001–M-050), Internal Call Table, Startup Sequence, and Async Boundaries are in `discovery/components/A02_module_call_map.md`. Summary:

- **Layer distribution:** serving (15: 5 web core + 10 routers) · pipeline (21: notebooks, orchestrator, AI provider clients, matching, mock ERP, normalization) · infra (14: lakehouse connection/migrations, audit logger, concurrency limiter, shop-owner routing, blob client, 7 test/harness scripts meeting the BCE-009 module-worthiness bar). 50 modules total.
- **Net change vs. the archived roster (48 modules):** +2 — `scripts/test_fabric_connection.py` (new, Fabric smoke test) and `tests/test_level2_matching_integration.py` (new, real-pipeline integration test proving Level 2/tolerance matching fires end-to-end, added alongside the `renumbered_invoices` mock-ERP exception type). Every module present in the archived roster is still present in this one under a freshly assigned M-NNN (numbering was not preserved — see the note at the top of the roster file on why this is a fresh baseline, not an incremental refresh).
- **The AI provider chain contradiction the archived TOPOLOGY.md flagged as resolved (`claude_sonnet` confirmed primary, 9 doc/comment locations corrected 2026-07-24) is confirmed still correct** — verified directly against `config/ai/active_provider.json`, `src/ai/client_factory.py`'s comments, and `src/ai/claude_sonnet_client.py`'s own docstring this session; no regression.
- **`src/pipeline/` and `src/validation/` remain empty packages** (`__init__.py` only) — confirmed directly this session (`ls` shows 0 bytes of logic in each). Same open question as the archived record: dead scaffolding or unbuilt placeholder — cannot be resolved from source.

---

## A03 — External System Boundary Map

| ID | External System | Called by | Auth mechanism | Error handling assumptions | Request/response |
|---|---|---|---|---|---|
| IP-001 | Claude Sonnet 4.6 (Anthropic, via Azure AI Foundry) — active primary extraction provider | M-025 | API key (`AZURE_CLAUDE_API_KEY`) + Azure Foundry endpoint routing | Never raises out of the client; any failure converts to a clean `AIResponse(success=False)`, causing `document_understanding_engine.py` (M-024) to fall through to pdfplumber (M-031). Streaming call required — a non-streaming request on a long whole-document extraction risks a read timeout. Gated by M-041's concurrency limiter (`VIVE_MAX_CONCURRENT_AI_CALLS`, default 2). | Whole PDF as one base64 document block; JSON response with `rows`/`columns_found`/`vendor_name`/`statement_date`. |
| IP-002 | Claude Haiku 4.5 (Anthropic, via Azure AI Foundry) — used exclusively by M-033 (Explanation Service), independent of `provider_chain` | M-026 | API key (`AZURE_CLAUDE_API_KEY`, same as IP-001, different deployment env var) | Same clean-failure pattern; `explain_all_open_exceptions()` marks that exception `failed` and continues to the next one. | Text-only prompt, JSON response (`probable_cause`, `suggested_resolution`, `confidence_score`, `business_impact`). |
| IP-003 | Azure OpenAI (gpt-5-mini / gpt-5-nano / gpt-5.1, Responses API) — registered, not in the active chain | M-027 (reachable only via explicit `get_ai_client("azure_gpt5_*")`) | API key + endpoint + deployment name env vars | Rejects `temperature` outright (400) for these reasoning models. Per-page calls (whole-document call was found to time out/silently narrow scope in prior testing). One retry per page before recording a failed page. | One Responses API call per PDF page; `input_file`/`input_image` content blocks. |
| IP-004 | Azure Document Intelligence (`prebuilt-layout`) — registered, not in the active chain | M-028 (reachable only via explicit `get_ai_client("azure_doc_intel")`) | API key + endpoint env vars | Whole PDF in one call; a headerless continuation table whose column count doesn't match the last detected header is skipped with a warning rather than risking misaligned data. | One call analyzes the full document; returns generic table geometry (rows/cells), mapped via pdfplumber's shared column-header interpreter (M-031). |
| IP-005 | Google Gemini 2.5 Flash (google-genai SDK) — registered, not in the active chain | M-029 (reachable only via explicit `get_ai_client("gemini")`) | API key (`GEMINI_API_KEY`) | Retries up to `max_retries` times (default 2) specifically for 503 UNAVAILABLE, 60s wait between attempts; any other error fails immediately with no retry. | File upload (Files API) + one `generate_content` call; JSON response with `rows`/`columns_found`. |
| IP-006 | Mistral Medium (direct Mistral API, OpenAI-compatible) — registered, not in the active chain | M-030 (reachable only via explicit `get_ai_client("mistral")`) | API key (`MISTRAL_API_KEY`) + endpoint env var | Cannot accept raw PDF data URIs — every page is rasterized to PNG first. One retry per page. | One chat-completions call per rasterized page image; JSON response with `raw`/`mapped` per row. |
| IP-007 | Tesseract OCR + Poppler (local binaries, via pytesseract/pdf2image) — last-resort text extraction | M-032, called by M-031 | None (local binary, no network auth) | `is_ocr_available()` checked once per pdfplumber-fallback call, not per-page. OCR-derived rows get `line_confidence = 0.50`, deliberately below the 0.60 validation threshold, so they always route to human review. | Rasterizes one PDF page at a time (dpi=200 by default); returns raw OCR text, converted to a pseudo-table. |
| IP-008 | Lakehouse database — SQLite (local/dev/test) or Azure SQL (production), selected by `AZURE_SQL_SERVER` env var | Nearly every module via M-037 (M-003, M-005 indirectly via M-003, M-016, M-017, M-019 via M-034, M-020, M-034, M-035, M-039, M-040) | Azure SQL: username/password via pyodbc connection string, TLS encrypted. SQLite: none (local file). | Azure SQL serverless auto-pause can drop a long-running connection (SQLSTATE 08S01/08001) — `_run_with_retry()` retries up to 3 times with a fresh connection and a 5s wait. | `execute_sql()`/`execute_query()` abstract the two dialects (`INSERT OR REPLACE` → `MERGE`, trailing `LIMIT` → `SELECT TOP`) — see `_translate_for_azure()`. |
| IP-009 | Azure Blob Storage (`vendor-statements` container) — PDF archival | M-043, called by M-017 | Connection string (`AZURE_BLOB_CONNECTION_STRING` env var) | Never raises — missing config, missing file, or any SDK/network failure all return `None`; caller logs a warning and continues. | One blob upload per PDF, keyed `{vendor_slug}/{yyyy}/{mm}/{document_hash}.pdf`, metadata attached. |
| IP-010 | Azure Event Grid (auto-intake webhook, `viverecondropzone` storage account / `incoming-statements` container) — inbound trigger for M-015 | M-015 (inbound HTTP POST from Azure Event Grid, not called by our code) | Shared secret (`VIVE_EVENTGRID_WEBHOOK_SECRET`), checked via constant-time comparison against a static delivery header, before anything else in the handler runs — confirmed still fail-closed (401) if unset. | Download side is hard-pinned to the configured dropzone container regardless of what container the inbound event's blob URL names. A hard cap of 100 events per delivery is enforced. **Still not deployed** — the actual Azure Event Grid subscription has not yet been configured (blocked on Azure RBAC permissions per the archived record; not re-verified as unblocked this session). | Downloaded PDFs are queued as jobs identically to a manual upload, tagged `submitted_by="event-grid"` and a shared `batch_id` per delivery. |
| IP-011 | **[NEW]** Microsoft Fabric Warehouse — `get_fabric_connection()`, Azure CLI token auth (`AzureCliCredential` + `SQL_COPT_SS_ACCESS_TOKEN`) | M-037 itself; transitively, every caller of `execute_sql_fabric()`/`execute_query_fabric()` (M-003, M-017, M-045, M-046) | Azure CLI-issued token, reusing an existing `az login` session — not the ODBC driver's own `Authentication=ActiveDirectoryInteractive` (that path failed with FA004/0x534 on this machine per the code comment). Requires `az login` to have been run once already in the environment. | Falls back to the **same local SQLite `DB_PATH`** as `get_connection()` whenever `AZURE_SQL_SERVER` is unset — a deliberate test-isolation convention (confirmed at `src/lakehouse/connection.py:75-90`), not a separate dev database. No dialect translation and no connection-drop retry on the real Fabric path (unlike `execute_sql()`/`execute_query()`'s Azure SQL retry logic) — callers must write SQL valid on both SQLite and T-SQL. | One connection per call (`execute_sql_fabric()`/`execute_query_fabric()` each open and close their own), unlike `execute_sql()`/`execute_query()`'s shared `_run_with_retry()` wrapper. Currently cut over for `extraction_cache`, `document_intake_log`, `validation_document_review_queue` — see A01 row 8 and the STAGE-2-DIVERGENCE note above. |

---

## Engineer Review Required Before Sessions F, B, C, G, U Begin

1. **[Carried forward from archive, still open]** Confirm the methodology adaptation for UI layer classification — server-rendered FastAPI/Jinja2 treated as `serving`, no `route`/`page`/`layout`/`component`/`store` split applied (PBVI-011's taxonomy assumes a client-side SPA).
2. **[NEW]** Confirm the actual, current scope of the Fabric Warehouse cut-over (three tables, not one — see STAGE-2-DIVERGENCE above) and whether the unmitigated concurrency-unsafe `MAX(id)+1` pattern on those three tables (A01 row 8) should become a tracked risk before Session E's cross-artifact review.
3. **[Carried forward, confirmed still true]** `src/pipeline/` and `src/validation/` remain empty packages — confirm whether this is intentional scaffolding for future work or dead code.
4. **[Carried forward, confirmed still open]** Whether the missing stale-job requeue logic (A01 row 1) is a known, accepted gap or should become a RISK_REGISTER item ahead of broader multi-user rollout.
5. **[NEW — not re-verified this session]** IP-010's deployment status (Event Grid subscription configuration, blocked on Azure RBAC permissions per the archived record) was not re-confirmed as still blocked or since unblocked — flag for the engineer to state current status rather than assume either way.

`TOPOLOGY.md` and `discovery/ID_REGISTRY.md` must be committed and engineer-reviewed before Session F (Domain Model) and Sessions B, C, G, and U begin.
