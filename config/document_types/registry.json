{
  "_comment": "Declares which document types the system recognizes and which are wired into the reconciliation pipeline today. Others are stored in Bronze but parked — no downstream consumer yet.",
  "types": {
    "VENDOR_STATEMENT": {
      "active_for_reconciliation": true,
      "description": "Monthly vendor statement listing unpaid invoices"
    },
    "INVOICE": {
      "active_for_reconciliation": false,
      "description": "Individual invoice document"
    },
    "CREDIT_MEMO": {
      "active_for_reconciliation": false,
      "description": "Credit memo / return"
    },
    "PURCHASE_ORDER": {
      "active_for_reconciliation": false,
      "description": "Purchase order"
    },
    "PAYMENT_VOUCHER": {
      "active_for_reconciliation": false,
      "description": "Payment voucher / remittance"
    },
    "UNKNOWN": {
      "active_for_reconciliation": false,
      "description": "Unrecognized document type — always routes to review queue"
    }
  },
  "routing": {
    "document_type_confidence_threshold": 0.70,
    "low_confidence_action": "REVIEW_QUEUE"
  }
}
